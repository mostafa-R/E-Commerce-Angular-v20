import { CreditFormatPipe } from './credit-format-pipe';

describe('CreditFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
